// with return type and with argument.
#include<stdio.h>
 void sum(int,int);
 int main()
{
int a,b;
printf("enter the values");
scanf("%d%d",&a,&b);
c=sum(a,b);
 };
 void sum(int x,int y){
 int sum=0,a,b,c;
 c=x+y;
 return c;
 }
 
